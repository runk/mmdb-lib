import Reader from './lib'; // Reader is equal to {default: ActuallyReader}

// import {default as Reader} from 'mmdb-lib'; // Reader is equal to {default: ActuallyReader} because default is a special keyword here